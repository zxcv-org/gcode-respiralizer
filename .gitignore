# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
#
# For gcode-respiralizer, keep any/all Cargo.lock(s) in git. The worst downside seems to be
# potentially including a Cargo.lock that gets ignored by direct clients of the lib, but
# upsides include better reproducibility of builds, which can be quite helpful for
# bisecting regressions.
#Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# The author of this repo currently uses vscode.
.vscode
